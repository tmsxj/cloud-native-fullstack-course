# 模块21：主流云厂商产品实战

> 国内外主流云厂商核心产品实战指南，涵盖AWS、Azure、阿里云、腾讯云、华为云。
> 
> **适用场景**: 云架构选型、多云部署、成本优化、跨云迁移
> **核心价值**: 一通百通，掌握一家云厂商后快速上手其他厂商
> **价格更新**: 本模块价格表最后更新于 2025-Q1，下次更新 2025-Q2

---

## 目录

1. [云厂商概览与选型](#1-云厂商概览与选型)
2. [AWS实战](#2-aws实战)
3. [Azure实战](#3-azure实战)
4. [Google Cloud实战](#4-google-cloud实战)
5. [阿里云实战](#5-阿里云实战)
6. [腾讯云实战](#6-腾讯云实战)
7. [华为云实战](#7-华为云实战)
8. [跨云对比与迁移](#8-跨云对比与迁移)
9. [价格参考表](#9-价格参考表)
10. [附录：CLI工具速查](#10-附录cli工具速查)

---

## 1. 云厂商概览与选型

### 1.1 全球云厂商市场份额（2024）

| 排名 | 云厂商 | 全球份额 | 优势区域 | 核心优势 |
|------|--------|----------|----------|----------|
| 1 | **AWS** | 31% | 全球 | 产品最全、生态最成熟 |
| 2 | **Azure** | 24% | 全球、企业 | 微软生态整合、混合云 |
| 3 | **Google Cloud** | 11% | 全球、AI | AI/ML、Kubernetes |
| 4 | **阿里云** | 4% | 亚太、中国 | 国内第一、电商基因 |
| 5 | **华为云** | 2% | 中国、政务 | 政企市场、自主可控 |
| 6 | **腾讯云** | 2% | 中国、游戏 | 社交/游戏、音视频 |

### 1.2 产品命名对照表

| 功能 | AWS | Azure | Google Cloud | 阿里云 | 腾讯云 | 华为云 |
|------|-----|-------|--------------|--------|--------|--------|
| **虚拟机** | EC2 | Virtual Machine | Compute Engine | ECS | CVM | ECS |
| **对象存储** | S3 | Blob Storage | Cloud Storage | OSS | COS | OBS |
| **块存储** | EBS | Disk | Persistent Disk | ESSD | CBS | EVS |
| **负载均衡** | ELB/ALB | Load Balancer | Cloud Load Balancing | SLB | CLB | ELB |
| **VPC网络** | VPC | Virtual Network | VPC | VPC | VPC | VPC |
| **关系数据库** | RDS | SQL Database | Cloud SQL | RDS | TencentDB | RDS |
| **NoSQL** | DynamoDB | Cosmos DB | Firestore | Table Store | TDSQL | GaussDB |
| **Redis** | ElastiCache | Cache for Redis | Memorystore | Redis | Redis | DCS |
| **容器服务** | EKS | AKS | GKE | ACK | TKE | CCE |
| **Serverless** | Lambda | Functions | Cloud Functions | Function Compute | SCF | FunctionGraph |
| **监控** | CloudWatch | Monitor | Cloud Monitoring | CloudMonitor | CM | AOM |
| **日志** | CloudWatch Logs | Log Analytics | Cloud Logging | SLS | CLS | LTS |

### 1.3 选型决策树

```
业务部署区域？
├── 海外为主
│   ├── 需要最全产品 → AWS
│   ├── 微软生态企业 → Azure
│   └── AI/大数据 → Google Cloud
├── 国内为主
│   ├── 电商/互联网 → 阿里云
│   ├── 政企/政务 → 华为云
│   ├── 游戏/社交 → 腾讯云
│   └── 多云策略 → 阿里云+腾讯云
└── 全球部署
    └── AWS + 阿里云/华为云（国内节点）
```

### 1.4 免费额度对比

| 云厂商 | 免费时长 | 核心免费产品 |
|--------|----------|--------------|
| **AWS** | 12个月 | EC2 t2.micro 750小时/月、S3 5GB、RDS 750小时 |
| **Azure** | 12个月 | VM B1s 750小时/月、Blob 5GB、SQL Database 250GB |
| **Google Cloud** | 12个月+部分长期 | Compute Engine e2-micro 750小时/月、Cloud Storage 5GB、BigQuery 1TB查询/月 |
| **阿里云** | 部分产品长期 | ECS 1核1G 3个月、OSS 10GB、RDS 1个月 |
| **腾讯云** | 部分产品长期 | CVM 1核1G 1个月、COS 50GB、轻量服务器1年 |
| **华为云** | 部分产品长期 | ECS 1核1G 1个月、OBS 5GB、部分AI服务 |

---

## 2. AWS实战

### 2.1 账号与计费管理

#### 2.1.1 注册与IAM配置

```bash
# 安装AWS CLI
# Windows: https://awscli.amazonaws.com/AWSCLIV2.msi
# 验证安装
aws --version
# aws-cli/2.15.x

# 配置凭证(IAM用户密钥)
aws configure
# AWS Access Key ID [None]: AKIA...
# AWS Secret Access Key [None]: ...
# Default region name [None]: ap-northeast-1
# Default output format [None]: json

# 验证配置
aws sts get-caller-identity
```

#### 2.1.2 预算告警设置

```bash
# 创建预算（每月100美元告警）
aws budgets create-budget \
    --account-id $(aws sts get-caller-identity --query Account --output text) \
    --budget file://budget.json \
    --notifications-with-subscribers file://notifications.json

# budget.json
{
    "BudgetName": "MonthlyBudget",
    "BudgetLimit": {
        "Amount": "100",
        "Unit": "USD"
    },
    "TimeUnit": "MONTHLY",
    "BudgetType": "COST"
}
```

### 2.2 网络基础（VPC）

#### 2.2.1 创建VPC

```bash
# 创建VPC(虚拟私有云)
VPC_ID=$(aws ec2 create-vpc \
    --cidr-block 10.0.0.0/16  # CIDR网段 \
    --tag-specifications 'ResourceType=vpc,Tags=[{Key=Name,Value=ProductionVPC}]' \
    --query 'Vpc.VpcId' --output text)

# 创建子网(多可用区，AZ)
SUBNET_1A=$(aws ec2 create-subnet \
    --vpc-id $VPC_ID \
    --cidr-block 10.0.1.0/24 \
    --availability-zone ap-northeast-1a  # 可用区 \
    --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=Public-1a}]' \
    --query 'Subnet.SubnetId' --output text)

SUBNET_1C=$(aws ec2 create-subnet \
    --vpc-id $VPC_ID \
    --cidr-block 10.0.2.0/24 \
    --availability-zone ap-northeast-1c \
    --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=Public-1c}]' \
    --query 'Subnet.SubnetId' --output text)

# 创建Internet Gateway(互联网网关)
IGW_ID=$(aws ec2 create-internet-gateway \
    --tag-specifications 'ResourceType=internet-gateway,Tags=[{Key=Name,Value=ProdIGW}]' \
    --query 'InternetGateway.InternetGatewayId' --output text)

aws ec2 attach-internet-gateway --internet-gateway-id $IGW_ID --vpc-id $VPC_ID

# 创建路由表
ROUTE_TABLE_ID=$(aws ec2 create-route-table \
    --vpc-id $VPC_ID \
    --tag-specifications 'ResourceType=route-table,Tags=[{Key=Name,Value=PublicRT}]' \
    --query 'RouteTable.RouteTableId' --output text)

# 添加默认路由
aws ec2 create-route --route-table-id $ROUTE_TABLE_ID \
    --destination-cidr-block 0.0.0.0/0 --gateway-id $IGW_ID

# 关联子网
aws ec2 associate-route-table --route-table-id $ROUTE_TABLE_ID --subnet-id $SUBNET_1A
aws ec2 associate-route-table --route-table-id $ROUTE_TABLE_ID --subnet-id $SUBNET_1C

echo "VPC created: $VPC_ID"
```

#### 2.2.2 安全组配置

```bash
# 创建安全组(Security Group，虚拟防火墙)
SG_ID=$(aws ec2 create-security-group \
    --group-name WebServerSG \
    --description "Security group for web servers" \
    --vpc-id $VPC_ID \
    --query 'GroupId' --output text)

# 添加入站规则
aws ec2 authorize-security-group-ingress \
    --group-id $SG_ID \
    --protocol tcp --port 22 --cidr 0.0.0.0/0

aws ec2 authorize-security-group-ingress \
    --group-id $SG_ID \
    --protocol tcp --port 80 --cidr 0.0.0.0/0

aws ec2 authorize-security-group-ingress \
    --group-id $SG_ID \
    --protocol tcp --port 443 --cidr 0.0.0.0/0

# 查看安全组规则
aws ec2 describe-security-groups --group-ids $SG_ID
```

### 2.3 计算服务（EC2）

#### 2.3.1 创建EC2实例

```bash
# 获取最新AMI(亚马逊机器镜像)
AMI_ID=$(aws ec2 describe-images \
    --owners amazon \
    --filters 'Name=name,Values=amzn2-ami-hvm-*-x86_64-gp2' \
    --query 'sort_by(Images, &CreationDate)[-1].ImageId' --output text)

# 创建密钥对
aws ec2 create-key-pair --key-name MyKeyPair --query 'KeyMaterial' --output text > MyKeyPair.pem
chmod 400 MyKeyPair.pem

# 启动EC2实例
INSTANCE_ID=$(aws ec2 run-instances \
    --image-id $AMI_ID \
    --count 1 \
    --instance-type t3.medium  # 实例规格 \
    --key-name MyKeyPair \
    --security-group-ids $SG_ID \
    --subnet-id $SUBNET_1A \
    --tag-specifications 'ResourceType=instance,Tags=[{Key=Name,Value=WebServer}]' \
    --query 'Instances[0].InstanceId' --output text)

# 等待实例运行
aws ec2 wait instance-running --instance-ids $INSTANCE_ID

# 获取公网IP
PUBLIC_IP=$(aws ec2 describe-instances \
    --instance-ids $INSTANCE_ID \
    --query 'Reservations[0].Instances[0].PublicIpAddress' --output text)

echo "Instance created: $INSTANCE_ID, IP: $PUBLIC_IP"

# SSH连接
ssh -i MyKeyPair.pem ec2-user@$PUBLIC_IP
```

#### 2.3.2 自动伸缩配置

```bash
# 创建启动模板
aws ec2 create-launch-template \
    --launch-template-name WebServerTemplate \
    --launch-template-data '{
        "ImageId": "'$AMI_ID'",
        "InstanceType": "t3.medium",
        "KeyName": "MyKeyPair",
        "SecurityGroupIds": ["'$SG_ID'"],
        "UserData": "'$(echo '#!/bin/bash
yum update -y
yum install -y httpd
systemctl start httpd
systemctl enable httpd
echo "<h1>Hello from AWS</h1>" > /var/www/html/index.html' | base64 -w 0)'"
    }'

# 创建ASG(自动伸缩组)
aws autoscaling create-auto-scaling-group \
    --auto-scaling-group-name WebServerASG \
    --launch-template LaunchTemplateName=WebServerTemplate,Version='$Latest' \
    --min-size 2  # 最小实例数 \
    --max-size 6  # 最大实例数 \
    --desired-capacity 2  # 期望实例数 \
    --vpc-zone-identifier "$SUBNET_1A,$SUBNET_1C" \
    --target-group-arns $TARGET_GROUP_ARN

# 配置伸缩策略
aws autoscaling put-scaling-policy \
    --auto-scaling-group-name WebServerASG \
    --policy-name TargetTrackingPolicy \
    --policy-type TargetTrackingScaling \
    --target-tracking-configuration '{
        "PredefinedMetricSpecification": {
            "PredefinedMetricType": "ASGAverageCPUUtilization"
        },
        "TargetValue": 60.0
    }'
```

### 2.4 负载均衡（ALB）

```bash
# 创建ALB(应用负载均衡器)
ALB_ARN=$(aws elbv2 create-load-balancer \
    --name WebServerALB \
    --subnets $SUBNET_1A $SUBNET_1C \
    --security-groups $SG_ID \
    --scheme internet-facing \
    --type application \
    --query 'LoadBalancers[0].LoadBalancerArn' --output text)

# 创建目标组(Target Group)
TARGET_GROUP_ARN=$(aws elbv2 create-target-group \
    --name WebServerTG \
    --protocol HTTP \
    --port 80 \
    --vpc-id $VPC_ID \
    --health-check-path / \
    --query 'TargetGroups[0].TargetGroupArn' --output text)

# 创建监听器
aws elbv2 create-listener \
    --load-balancer-arn $ALB_ARN \
    --protocol HTTP \
    --port 80 \
    --default-actions Type=forward,TargetGroupArn=$TARGET_GROUP_ARN

# 获取ALB DNS
ALB_DNS=$(aws elbv2 describe-load-balancers \
    --load-balancer-arns $ALB_ARN \
    --query 'LoadBalancers[0].DNSName' --output text)

echo "ALB DNS: $ALB_DNS"
```

### 2.5 对象存储（S3）

```bash
# 创建S3存储桶(Bucket)
BUCKET_NAME="my-company-data-$(date +%s)"
aws s3 mb s3://$BUCKET_NAME --region ap-northeast-1

# 启用版本控制(Versioning)
aws s3api put-bucket-versionization \
    --bucket $BUCKET_NAME \
    --versioning-configuration Status=Enabled

# 设置生命周期策略
aws s3api put-bucket-lifecycle-configuration \
    --bucket $BUCKET_NAME \
    --lifecycle-configuration file://lifecycle.json

# lifecycle.json
{
    "Rules": [{
        "ID": "MoveToGlacier",
        "Status": "Enabled",
        "Filter": {"Prefix": "logs/"},
        "Transitions": [{
            "Days": 90,
            "StorageClass": "GLACIER"
        }]
    }]
}

# 上传文件
aws s3 cp local-file.txt s3://$BUCKET_NAME/

# 同步目录
aws s3 sync ./local-folder s3://$BUCKET_NAME/remote-folder

# 生成预签名URL（1小时有效）
aws s3 presign s3://$BUCKET_NAME/private-file.txt --expires-in 3600
```

### 2.6 数据库（RDS）

```bash
# 创建RDS子网组
aws rds create-db-subnet-group \
    --db-subnet-group-name MyDBSubnetGroup \
    --db-subnet-group-description "Subnet group for RDS" \
    --subnet-ids '["'$SUBNET_1A'","'$SUBNET_1C'"]'

# 创建安全组
DB_SG_ID=$(aws ec2 create-security-group \
    --group-name DatabaseSG \
    --description "Security group for database" \
    --vpc-id $VPC_ID \
    --query 'GroupId' --output text)

# 允许Web服务器访问数据库
aws ec2 authorize-security-group-ingress \
    --group-id $DB_SG_ID \
    --protocol tcp --port 3306 --source-group $SG_ID

# 创建RDS(关系型数据库服务)实例
aws rds create-db-instance \
    --db-instance-identifier my-mysql-db \
    --db-instance-class db.t3.micro  # 实例规格 \
    --engine mysql  # 数据库引擎 \
    --engine-version 8.0  # 数据库版本 \
    --allocated-storage 20 \
    --storage-type gp2 \
    --master-username admin \
    --master-user-password 'YourStrongPassword123!' \
    --vpc-security-group-ids $DB_SG_ID \
    --db-subnet-group-name MyDBSubnetGroup \
    --publicly-accessible false \
    --backup-retention-period 7 \
    --tags Key=Name,Value=ProductionDB

# 等待数据库可用
aws rds wait db-instance-available --db-instance-identifier my-mysql-db

# 获取Endpoint
DB_ENDPOINT=$(aws rds describe-db-instances \
    --db-instance-identifier my-mysql-db \
    --query 'DBInstances[0].Endpoint.Address' --output text)

echo "Database Endpoint: $DB_ENDPOINT"
```

### 2.7 监控与告警（CloudWatch）

```bash
# 创建CloudWatch告警（CPU使用率超过80%）
aws cloudwatch put-metric-alarm \
    --alarm-name HighCPUAlarm  # 告警名称 \
    --alarm-description "Alarm when CPU exceeds 80%" \
    --metric-name CPUUtilization \
    --namespace AWS/EC2 \
    --statistic Average \
    --period 300 \
    --threshold 80 \
    --comparison-operator GreaterThanThreshold \
    --dimensions Name=InstanceId,Value=$INSTANCE_ID \
    --evaluation-periods 2 \
    --alarm-actions arn:aws:sns:ap-northeast-1:123456789012:MyTopic

# 查看指标
aws cloudwatch get-metric-statistics \
    --namespace AWS/EC2 \
    --metric-name CPUUtilization \
    --dimensions Name=InstanceId,Value=$INSTANCE_ID \
    --start-time 2025-01-01T00:00:00Z \
    --end-time 2025-01-02T00:00:00Z \
    --period 3600 \
    --statistics Average
```

---

## 3. Azure实战

### 3.1 账号与计费管理

```bash
# 安装Azure CLI
# Windows: winget install Microsoft.AzureCLI

# 登录
az login

# 设置默认订阅
az account set --subscription "Your Subscription Name"

# 查看当前订阅
az account show
```

### 3.2 网络基础（Virtual Network）

```bash
# 创建资源组(Resource Group)
az group create \
    --name ProductionRG \
    --location japaneast

# 创建VNet(虚拟网络)
az network vnet create \
    --resource-group ProductionRG \
    --name ProductionVNet \
    --address-prefix 10.0.0.0/16 \
    --subnet-name PublicSubnet \
    --subnet-prefix 10.0.1.0/24

# 添加子网
az network vnet subnet create \
    --resource-group ProductionRG \
    --vnet-name ProductionVNet \
    --name PrivateSubnet \
    --address-prefix 10.0.2.0/24

# 创建NSG(网络安全组，Network Security Group)
az network nsg create \
    --resource-group ProductionRG \
    --name WebServerNSG

# 添加规则
az network nsg rule create \
    --resource-group ProductionRG \
    --nsg-name WebServerNSG \
    --name AllowSSH \
    --priority 100 \
    --source-address-prefixes '*' \
    --source-port-ranges '*' \
    --destination-address-prefixes '*' \
    --destination-port-ranges 22 \
    --access Allow \
    --protocol Tcp

az network nsg rule create \
    --resource-group ProductionRG \
    --nsg-name WebServerNSG \
    --name AllowHTTP \
    --priority 110 \
    --source-address-prefixes '*' \
    --source-port-ranges '*' \
    --destination-address-prefixes '*' \
    --destination-port-ranges 80 443 \
    --access Allow \
    --protocol Tcp
```

### 3.3 计算服务（Virtual Machine）

```bash
# 创建VM
az vm create \
    --resource-group ProductionRG \
    --name WebServerVM \
    --image Ubuntu2204 \
    --size Standard_B2s \
    --admin-username azureuser \
    --generate-ssh-keys \
    --vnet-name ProductionVNet \
    --subnet PublicSubnet \
    --nsg WebServerNSG \
    --public-ip-sku Standard \
    --tags environment=production

# 开放端口
az vm open-port \
    --resource-group ProductionRG \
    --name WebServerVM \
    --port 80 443

# 获取IP
az vm show \
    --resource-group ProductionRG \
    --name WebServerVM \
    --show-details \
    --query publicIps \
    --output tsv

# SSH连接
ssh azureuser@<public-ip>
```

### 3.4 负载均衡

```bash
# 创建负载均衡器
az network lb create \
    --resource-group ProductionRG \
    --name WebServerLB \
    --sku Standard \
    --public-ip-address WebServerLBIP \
    --public-ip-address-allocation Static \
    --frontend-ip-name FrontendPool \
    --backend-pool-name BackendPool

# 创建健康探测
az network lb probe create \
    --resource-group ProductionRG \
    --lb-name WebServerLB \
    --name HealthProbe \
    --protocol Http \
    --port 80 \
    --path /

# 创建负载均衡规则
az network lb rule create \
    --resource-group ProductionRG \
    --lb-name WebServerLB \
    --name HTTPRule \
    --protocol Tcp \
    --frontend-port 80 \
    --backend-port 80 \
    --frontend-ip-name FrontendPool \
    --backend-pool-name BackendPool \
    --probe-name HealthProbe
```

### 3.5 对象存储（Blob Storage）

```bash
# 创建存储账户
az storage account create \
    --name mystorageaccount123 \
    --resource-group ProductionRG \
    --location japaneast \
    --sku Standard_LRS \
    --kind StorageV2

# 获取连接字符串
CONNECTION_STRING=$(az storage account show-connection-string \
    --name mystorageaccount123 \
    --resource-group ProductionRG \
    --query connectionString \
    --output tsv)

# 创建容器
az storage container create \
    --name mycontainer \
    --connection-string $CONNECTION_STRING

# 上传文件
az storage blob upload \
    --container-name mycontainer \
    --file local-file.txt \
    --name remote-file.txt \
    --connection-string $CONNECTION_STRING

# 生成SAS令牌（1小时有效）
az storage blob generate-sas \
    --container-name mycontainer \
    --name remote-file.txt \
    --permissions r \
    --expiry $(date -u -d '1 hour' '+%Y-%m-%dT%H:%MZ') \
    --connection-string $CONNECTION_STRING \
    --output tsv
```

---

## 4. Google Cloud实战

> Google Cloud Platform（GCP）是全球第三大云厂商，核心优势在于AI/ML、Kubernetes原生支持、全球网络基础设施。
>
> **适用场景**: AI/大数据、容器化、全球化部署、Google生态整合
> **核心优势**: GKE（最强K8s托管服务）、BigQuery（数据仓库）、全球低延迟网络

### 4.1 账号与IAM配置

```bash
# 安装Google Cloud CLI (gcloud)
# Windows: https://cloud.google.com/sdk/docs/install#windows
# macOS: brew install --cask google-cloud-sdk
# Linux: https://cloud.google.com/sdk/docs/install#linux

# 验证安装
gcloud --version
# Google Cloud SDK 500.0.x

# 初始化配置（登录并选择项目）
gcloud init
# 按提示完成浏览器OAuth登录
# 选择或创建项目

# 设置默认项目和区域
gcloud config set project my-project-id
gcloud config set compute/region asia-east1
gcloud config set compute/zone asia-east1-a

# 验证配置
gcloud config list
gcloud auth list
```

#### 4.1.1 服务账号与密钥管理

```bash
# 创建服务账号（用于CI/CD或应用访问）
gcloud iam service-accounts create my-sa \
    --display-name="My Service Account" \
    --description="For CI/CD deployment"

# 为服务账号授予角色
gcloud projects add-iam-policy-binding my-project-id \
    --member="serviceAccount:my-sa@my-project-id.iam.gserviceaccount.com" \
    --role="roles/compute.instanceAdmin"

# 创建并下载服务账号密钥（JSON格式）
gcloud iam service-accounts keys create ~/my-sa-key.json \
    --iam-account=my-sa@my-project-id.iam.gserviceaccount.com

# 激活服务账号（非交互式场景）
export GOOGLE_APPLICATION_CREDENTIALS=~/my-sa-key.json
gcloud auth activate-service-account \
    --key-file=$GOOGLE_APPLICATION_CREDENTIALS
```

#### 4.1.2 预算告警设置

```bash
# 创建预算（每月100美元告警）
gcloud billing budgets create \
    --billing-account=XXXXXX-XXXXXX-XXXXXX \
    --display-name="Monthly Budget Alert" \
    --budget-amount=100USD \
    --threshold-rule=percent=80 \
    --threshold-rule=percent=100 \
    --all-updates-rule-pubsub-topic=projects/my-project/topics/budget-alerts

# 查看预算
gcloud billing budgets list --billing-account=XXXXXX-XXXXXX-XXXXXX
```

### 4.2 网络基础（VPC）

#### 4.2.1 创建VPC网络

```bash
# 创建自定义VPC网络（推荐，可自定义子网）
gcloud compute networks create my-vpc \
    --subnet-mode=custom \
    --bgp-routing-mode=global \
    --description="Production VPC"

# 创建子网（多区域部署）
gcloud compute networks subnets create subnet-asia-east \
    --network=my-vpc \
    --region=asia-east1 \
    --range=10.0.1.0/24 \
    --enable-private-ip-google-access

gcloud compute networks subnets create subnet-asia-southeast \
    --network=my-vpc \
    --region=asia-southeast1 \
    --range=10.0.2.0/24 \
    --enable-private-ip-google-access

# 创建防火墙规则（允许SSH、HTTP、HTTPS）
gcloud compute firewall-rules create allow-ssh \
    --network=my-vpc \
    --allow=tcp:22 \
    --source-ranges=0.0.0.0/0 \
    --target-tags=allow-ssh

gcloud compute firewall-rules create allow-http-https \
    --network=my-vpc \
    --allow=tcp:80,tcp:443 \
    --source-ranges=0.0.0.0/0 \
    --target-tags=allow-http-https

# 查看VPC详情
gcloud compute networks describe my-vpc
gcloud compute networks subnets list --network=my-vpc
```

#### 4.2.2 Cloud NAT（出站网络地址转换）

```bash
# 创建Cloud Router（NAT需要）
gcloud compute routers create my-router \
    --network=my-vpc \
    --region=asia-east1

# 创建Cloud NAT（让私有子网实例访问互联网）
gcloud compute routers nats create my-nat \
    --router=my-router \
    --region=asia-east1 \
    --auto-allocate-nat-external-ips \
    --nat-all-subnet-ip-ranges

# 验证NAT配置
gcloud compute routers nats list --router=my-router --region=asia-east1
```

### 4.3 计算服务（Compute Engine）

#### 4.3.1 创建VM实例

```bash
# 创建VM实例（使用gcloud命令）
gcloud compute instances create my-vm \
    --zone=asia-east1-a \
    --machine-type=e2-medium \
    --image-family=ubuntu-2204-lts \
    --image-project=ubuntu-os-cloud \
    --boot-disk-size=20GB \
    --boot-disk-type=pd-balanced \
    --network=my-vpc \
    --subnet=subnet-asia-east \
    --tags=allow-ssh,allow-http-https \
    --metadata=startup-script='#!/bin/bash
        apt-get update
        apt-get install -y nginx
        systemctl start nginx'

# 获取实例外部IP
EXTERNAL_IP=$(gcloud compute instances describe my-vm \
    --zone=asia-east1-a \
    --format='get(networkInterfaces[0].accessConfigs[0].natIP)')

echo "VM created, IP: $EXTERNAL_IP"

# SSH连接（gcloud自动处理密钥）
gcloud compute ssh my-vm --zone=asia-east1-a

# 查看实例列表
gcloud compute instances list
```

#### 4.3.2 托管实例组（MIG，类似AWS Auto Scaling）

```bash
# 创建实例模板
gcloud compute instance-templates create my-template \
    --machine-type=e2-medium \
    --image-family=ubuntu-2204-lts \
    --image-project=ubuntu-os-cloud \
    --boot-disk-size=20GB \
    --network=my-vpc \
    --subnet=subnet-asia-east \
    --tags=allow-http-https \
    --metadata-from-file startup-script=startup.sh

# 创建托管实例组（MIG）
gcloud compute instance-groups managed create my-mig \
    --template=my-template \
    --size=2 \
    --zone=asia-east1-a

# 配置自动伸缩
gcloud compute instance-groups managed set-autoscaling my-mig \
    --max-num-replicas=10 \
    --min-num-replicas=2 \
    --target-cpu-utilization=0.6 \
    --cool-down-period=60 \
    --zone=asia-east1-a

# 配置健康检查
gcloud compute health-checks create http my-health-check \
    --port=80 \
    --request-path=/health

gcloud compute instance-groups managed set-health-checks my-mig \
    --health-check=my-health-check \
    --zone=asia-east1-a
```

#### 4.3.3 实例调度与抢占式实例（Preemptible）

```bash
# 创建抢占式实例（价格约为普通实例的20-60%，适合批处理任务）
gcloud compute instances create preemptible-vm \
    --zone=asia-east1-a \
    --machine-type=e2-standard-4 \
    --preemptible \
    --image-family=ubuntu-2204-lts \
    --image-project=ubuntu-os-cloud

# 查看实例调度选项
gcloud compute instances describe my-vm --zone=asia-east1-a \
    --format='table(scheduling.onHostMaintenance,scheduling.automaticRestart,scheduling.preemptible)'
```

### 4.4 存储服务

#### 4.4.1 Cloud Storage（对象存储）

```bash
# 创建存储桶（Bucket）
gcloud storage buckets create gs://my-bucket-name \
    --location=asia-east1 \
    --storage-class=STANDARD \
    --uniform-bucket-level-access

# 上传文件
gcloud storage cp local-file.txt gs://my-bucket-name/
gcloud storage cp -r local-folder/ gs://my-bucket-name/folder/

# 下载文件
gcloud storage cp gs://my-bucket-name/file.txt ./

# 设置生命周期规则（自动删除旧文件）
gcloud storage buckets update gs://my-bucket-name \
    --lifecycle-file=lifecycle.json

# lifecycle.json 示例
{
  "rule": [
    {
      "action": {"type": "Delete"},
      "condition": {"age": 30}
    }
  ]
}

# 查看存储桶列表
gcloud storage ls
gcloud storage ls gs://my-bucket-name/

# 设置公开访问（谨慎使用）
gcloud storage buckets add-iam-policy-binding gs://my-bucket-name \
    --member=allUsers \
    --role=roles/storage.objectViewer
```

#### 4.4.2 Persistent Disk（块存储）

```bash
# 创建Persistent Disk
gcloud compute disks create my-disk \
    --size=100GB \
    --type=pd-ssd \
    --zone=asia-east1-a

# 挂载到现有VM
gcloud compute instances attach-disk my-vm \
    --disk=my-disk \
    --zone=asia-east1-a

# 在VM内格式化和挂载
# sudo mkfs.ext4 /dev/sdb
# sudo mkdir /mnt/data
# sudo mount /dev/sdb /mnt/data

# 创建快照（备份）
gcloud compute disks snapshot my-disk \
    --snapshot-names=my-disk-snapshot \
    --zone=asia-east1-a

# 从快照创建新磁盘
gcloud compute disks create my-disk-restored \
    --source-snapshot=my-disk-snapshot \
    --zone=asia-east1-a
```

### 4.5 负载均衡（Cloud Load Balancing）

```bash
# 创建健康检查
gcloud compute health-checks create http my-health-check \
    --port=80 \
    --request-path=/health

# 创建后端服务（关联MIG）
gcloud compute backend-services create my-backend \
    --protocol=HTTP \
    --health-checks=my-health-check \
    --global

gcloud compute backend-services add-backend my-backend \
    --instance-group=my-mig \
    --instance-group-zone=asia-east1-a \
    --global

# 创建URL映射（路由规则）
gcloud compute url-maps create my-url-map \
    --default-service=my-backend

# 创建SSL证书（使用Google托管证书）
gcloud compute ssl-certificates create my-ssl-cert \
    --domains=example.com,www.example.com \
    --global

# 创建HTTPS代理
gcloud compute target-https-proxies create my-https-proxy \
    --ssl-certificates=my-ssl-cert \
    --url-map=my-url-map

# 创建全局转发规则（分配全球任播IP）
gcloud compute forwarding-rules create my-forwarding-rule \
    --global \
    --target-https-proxy=my-https-proxy \
    --ports=443

# 查看转发规则分配的IP
EXTERNAL_IP=$(gcloud compute forwarding-rules describe my-forwarding-rule \
    --global --format='get(IPAddress)')
echo "Load Balancer IP: $EXTERNAL_IP"
```

### 4.6 数据库服务

#### 4.6.1 Cloud SQL（托管关系型数据库）

```bash
# 创建Cloud SQL实例（MySQL）
gcloud sql instances create my-mysql \
    --database-version=MYSQL_8_0 \
    --tier=db-f1-micro \
    --region=asia-east1 \
    --storage-size=10GB \
    --storage-auto-increase \
    --availability-type=REGIONAL \
    --backup-start-time=03:00

# 设置root密码
gcloud sql users set-password root \
    --host=% \
    --instance=my-mysql \
    --password=YourStrongPassword

# 创建数据库
gcloud sql databases create mydb --instance=my-mysql

# 创建用户
gcloud sql users create myuser \
    --host=% \
    --instance=my-mysql \
    --password=UserPassword

# 查看实例连接信息
gcloud sql instances describe my-mysql --format='table(connectionName, ipAddresses[0].ipAddress)'

# 连接数据库（Cloud SQL Auth Proxy方式，推荐用于生产）
# 下载Cloud SQL Proxy: https://cloud.google.com/sql/docs/mysql/sql-proxy
./cloud-sql-proxy my-project:asia-east1:my-mysql &
mysql -u root -p -h 127.0.0.1

# 查看实例列表
gcloud sql instances list
```

#### 4.6.2 Cloud SQL for PostgreSQL

```bash
# 创建PostgreSQL实例
gcloud sql instances create my-postgres \
    --database-version=POSTGRES_15 \
    --tier=db-f1-micro \
    --region=asia-east1 \
    --storage-size=10GB \
    --availability-type=REGIONAL

# 设置默认用户密码
gcloud sql users set-password postgres \
    --host=% \
    --instance=my-postgres \
    --password=YourStrongPassword

# 创建数据库
gcloud sql databases create myapp --instance=my-postgres
```

#### 4.6.3 Memorystore for Redis（托管Redis）

```bash
# 创建Redis实例
gcloud redis instances create my-redis \
    --tier=standard \
    --size=5 \
    --region=asia-east1 \
    --redis-version=redis_7_0 \
    --network=my-vpc \
    --connect-mode=PRIVATE_SERVICE_ACCESS

# 获取连接信息
REDIS_IP=$(gcloud redis instances describe my-redis \
    --region=asia-east1 \
    --format='get(host)')
REDIS_PORT=$(gcloud redis instances describe my-redis \
    --region=asia-east1 \
    --format='get(port)')

echo "Redis: $REDIS_IP:$REDIS_PORT"
```

### 4.7 容器服务（Google Kubernetes Engine, GKE）

#### 4.7.1 创建GKE标准集群

```bash
# 创建GKE标准集群（推荐用于生产）
gcloud container clusters create my-gke-cluster \
    --zone=asia-east1-a \
    --release-channel=regular \
    --machine-type=e2-medium \
    --num-nodes=3 \
    --disk-size=50GB \
    --disk-type=pd-balanced \
    --network=my-vpc \
    --subnetwork=subnet-asia-east \
    --enable-ip-alias \
    --enable-network-policy \
    --enable-autoscaling \
    --min-nodes=1 \
    --max-nodes=10 \
    --enable-autorepair \
    --enable-autoupgrade \
    --logging=SYSTEM,WORKLOAD \
    --monitoring=SYSTEM,WORKLOAD \
    --workload-pool=my-project-id.svc.id.goog  # 启用Workload Identity

# 获取集群凭证
kubectl
gcloud container clusters get-credentials my-gke-cluster --zone=asia-east1-a

# 验证连接
kubectl get nodes
kubectl get pods -A
```

#### 4.7.2 创建GKE Autopilot集群（全托管，按Pod计费）

```bash
# 创建GKE Autopilot集群（无需管理节点，按实际Pod资源计费）
gcloud container clusters create-auto my-autopilot-cluster \
    --region=asia-east1 \
    --release-channel=regular \
    --network=my-vpc \
    --subnetwork=subnet-asia-east \
    --enable-workload-identity \
    --enable-private-nodes

# 获取凭证
gcloud container clusters get-credentials my-autopilot-cluster --region=asia-east1

# Autopilot特点：
# - 无需管理节点池，Google自动管理底层基础设施
# - 按Pod实际请求的资源计费，而非节点整机
# - 自动应用安全最佳实践（如SecurityContext约束）
# - 适合工作负载波动大的场景
```

#### 4.7.3 GKE核心特性

```bash
# 启用Workload Identity（Pod以GCP服务账号身份访问GCP资源）
gcloud container node-pools update default-pool \
    --cluster=my-gke-cluster \
    --zone=asia-east1-a \
    --workload-metadata=GKE_METADATA

# 配置Pod使用GCP服务账号
# 创建K8s ServiceAccount与GCP ServiceAccount的绑定
cat <<EOF | kubectl apply -f -
apiVersion: v1
kind: ServiceAccount
metadata:
  name: my-ksa
  namespace: default
  annotations:
    iam.gke.io/gcp-service-account: my-sa@my-project-id.iam.gserviceaccount.com
EOF

# 绑定GCP IAM权限
gcloud iam service-accounts add-iam-policy-binding \
    my-sa@my-project-id.iam.gserviceaccount.com \
    --role=roles/iam.workloadIdentityUser \
    --member="serviceAccount:my-project-id.svc.id.goog[default/my-ksa]"

# 启用Cloud Operations for GKE（监控和日志）
gcloud container clusters update my-gke-cluster \
    --zone=asia-east1-a \
    --logging=SYSTEM,WORKLOAD,API_SERVER \
    --monitoring=SYSTEM,WORKLOAD

# 查看集群详情
gcloud container clusters describe my-gke-cluster --zone=asia-east1-a
```

### 4.8 Serverless（Cloud Functions & Cloud Run）

#### 4.8.1 Cloud Functions（函数计算）

```bash
# 部署HTTP触发函数（Python示例）
gcloud functions deploy my-http-function \
    --runtime=python311 \
    --trigger-http \
    --entry-point=hello_world \
    --memory=256MB \
    --timeout=60s \
    --max-instances=10 \
    --region=asia-east1 \
    --source=./functions/

# 查看函数详情
gcloud functions describe my-http-function --region=asia-east1

# 查看函数日志
gcloud functions logs read my-http-function --region=asia-east1 --limit=50

# 调用函数
FUNCTION_URL=$(gcloud functions describe my-http-function \
    --region=asia-east1 --format='get(httpsTrigger.url)')
curl $FUNCTION_URL
```

#### 4.8.2 Cloud Run（容器化Serverless）

```bash
# 构建并推送容器镜像到Artifact Registry
gcloud artifacts repositories create my-repo \
    --repository-format=docker \
    --location=asia-east1

# 配置Docker认证
gcloud auth configure-docker asia-east1-docker.pkg.dev

# 构建镜像
docker build -t asia-east1-docker.pkg.dev/my-project-id/my-repo/my-app:v1 .
docker push asia-east1-docker.pkg.dev/my-project-id/my-repo/my-app:v1

# 部署到Cloud Run
gcloud run deploy my-app \
    --image=asia-east1-docker.pkg.dev/my-project-id/my-repo/my-app:v1 \
    --region=asia-east1 \
    --platform=managed \
    --allow-unauthenticated \
    --memory=512Mi \
    --cpu=1 \
    --concurrency=80 \
    --max-instances=10 \
    --min-instances=1

# 查看服务URL
SERVICE_URL=$(gcloud run services describe my-app \
    --region=asia-east1 --format='get(status.url)')
echo "Service URL: $SERVICE_URL"

# Cloud Run特点：
# - 基于容器，支持任何语言/框架
# - 自动扩缩容到0（无请求时不计费）
# - 支持HTTP/2、gRPC
# - 内置HTTPS和自定义域名
```

### 4.9 监控与日志（Cloud Operations）

#### 4.9.1 Cloud Monitoring（指标监控）

```bash
# 创建自定义指标监控
gcloud monitoring dashboards create \
    --config-from-file=dashboard.json

# 创建告警策略
gcloud alpha monitoring policies create \
    --policy="displayName='High CPU Alert',
    conditions=[
      displayName='CPU > 80%',
      conditionThreshold={
        filter='resource.type=\"gce_instance\" AND metric.type=\"compute.googleapis.com/instance/cpu/utilization\"',
        aggregations=[alignmentPeriod=60s,perSeriesAligner=ALIGN_MEAN],
        comparison=COMPARISON_GT,
        thresholdValue=0.8,
        duration=300s
      }
    ],
    alertStrategy=notificationRateLimit={period=3600s},
    notificationChannels=['projects/my-project/notificationChannels/CHANNEL_ID']"

# 查看监控指标列表
gcloud monitoring metrics list --filter='metric.type:compute.googleapis.com'
```

#### 4.9.2 Cloud Logging（日志管理）

```bash
# 查看日志
gcloud logging read "resource.type=gce_instance AND severity>=ERROR" \
    --limit=50 \
    --format='table(timestamp,severity,textPayload)'

# 创建日志导出到Cloud Storage
gcloud logging sinks create my-sink gs://my-log-bucket \
    --log-filter='resource.type=gce_instance'

# 创建日志导出到BigQuery（用于分析）
gcloud logging sinks create my-bq-sink bigquery.googleapis.com/projects/my-project/datasets/my_logs \
    --log-filter='resource.type=k8s_container'

# 查看日志桶
gcloud logging buckets list
```

#### 4.9.3 Cloud Trace（分布式链路追踪）

```bash
# Cloud Trace自动集成GKE和Cloud Run，无需额外配置
# 查看最近的Trace
gcloud trace list --limit=20

# 查看特定服务的Trace
gcloud trace list --filter='service:my-app' --limit=10
```

### 4.10 IAM与权限管理

```bash
# 查看项目IAM策略
gcloud projects get-iam-policy my-project-id

# 为成员授予角色
gcloud projects add-iam-policy-binding my-project-id \
    --member=user:user@example.com \
    --role=roles/compute.instanceAdmin

# 为群组授予只读权限
gcloud projects add-iam-policy-binding my-project-id \
    --member=group:ops-team@example.com \
    --role=roles/viewer

# 创建自定义角色
gcloud iam roles create customComputeAdmin \
    --project=my-project-id \
    --title="Custom Compute Admin" \
    --description="Custom role for compute management" \
    --permissions=compute.instances.start,compute.instances.stop,compute.instances.reset \
    --stage=GA

# 查看预定义角色权限
gcloud iam roles describe roles/compute.instanceAdmin
```

### 4.11 其他核心服务速查

#### 4.11.1 BigQuery（数据仓库，按查询量计费）

```bash
# 创建数据集
gcloud bq datasets create my_dataset --location=asia-east1

# 查询公共数据集示例
bq query --use_legacy_sql=false \
    'SELECT name, COUNT(*) as count FROM `bigquery-public-data.usa_names.usa_1910_2013` GROUP BY name ORDER BY count DESC LIMIT 10'

# 加载数据到表
bq load --source_format=CSV my_dataset.my_table ./data.csv schema.json
```

#### 4.11.2 Pub/Sub（消息队列）

```bash
# 创建Topic
gcloud pubsub topics create my-topic

# 创建订阅
gcloud pubsub subscriptions create my-sub \
    --topic=my-topic \
    --ack-deadline=60 \
    --message-retention-duration=1d

# 发布消息
gcloud pubsub topics publish my-topic --message="Hello, Pub/Sub!"

# 拉取消息
gcloud pubsub subscriptions pull my-sub --auto-ack --limit=10
```

#### 4.11.3 Secret Manager（密钥管理）

```bash
# 创建密钥
echo -n "my-secret-value" | gcloud secrets create my-secret \
    --data-file=- \
    --replication-policy=automatic

# 查看密钥版本
gcloud secrets versions list my-secret

# 读取密钥（在VM或GKE中使用）
gcloud secrets versions access latest --secret=my-secret
```

### 4.12 GCP → 腾讯云概念映射

> 刷宝科技面试核心：理解GCP与腾讯云的产品对应关系

| GCP产品 | 腾讯云对应产品 | 说明 |
|---------|--------------|------|
| **Compute Engine** | CVM | 虚拟机，GCP的e2-micro对标腾讯云的S2.SMALL1 |
| **Cloud Storage** | COS | 对象存储，都支持标准/低频/归档存储类型 |
| **Persistent Disk** | CBS | 块存储，都支持SSD和高效云盘 |
| **VPC** | VPC | 虚拟私有云，概念完全一致 |
| **Cloud Load Balancing** | CLB | 负载均衡，GCP的全球任播IP是特色 |
| **Cloud SQL** | TencentDB | 托管数据库，都支持MySQL/PostgreSQL |
| **Memorystore** | Redis | 托管Redis |
| **GKE** | TKE | 托管K8s，GKE的Autopilot模式对标TKE Serverless |
| **Cloud Functions** | SCF | 函数计算 |
| **Cloud Run** | 云托管/容器服务 | 容器化Serverless，腾讯云无完全对标产品 |
| **Cloud Monitoring** | CM/云监控 | 指标监控 |
| **Cloud Logging** | CLS | 日志服务 |
| **Cloud Trace** | 无完全对标 | 分布式链路追踪，腾讯云需用SkyWalking自建 |
| **IAM** | CAM | 访问管理，GCP的Workload Identity是特色 |
| **Secret Manager** | 凭据管理系统 | 密钥管理服务 |
| **Artifact Registry** | TCR | 容器镜像仓库 |
| **Cloud NAT** | NAT网关 | 出站NAT |
| **Cloud Armor** | BGP高防包 | DDoS防护和WAF |

---

## 5. 阿里云实战

### 5.1 账号与RAM配置

```bash
# 安装阿里云CLI
# https://aliyun.com/product/cli

# 配置凭证(RAM访问密钥)
aliyun configure
# Access Key ID: LTAI...
# Access Key Secret: ...
# Default Region Id: cn-hangzhou
# Default Output Format: json

# 验证
aliyun sts GetCallerIdentity
```

### 4.2 网络基础（VPC）

```bash
# 创建VPC(专有网络)
VPC_ID=$(aliyun vpc CreateVpc \
    --RegionId cn-hangzhou \
    --CidrBlock 10.0.0.0/16 \
    --VpcName ProductionVPC \
    --Description "Production VPC" \
    --output text \
    --query VpcId)

# 创建交换机(子网，VSwitch)
VSWITCH_1A=$(aliyun vpc CreateVSwitch \
    --RegionId cn-hangzhou \
    --VpcId $VPC_ID \
    --ZoneId cn-hangzhou-a \
    --CidrBlock 10.0.1.0/24 \
    --VSwitchName Public-a \
    --output text \
    --query VSwitchId)

VSWITCH_1B=$(aliyun vpc CreateVSwitch \
    --RegionId cn-hangzhou \
    --VpcId $VPC_ID \
    --ZoneId cn-hangzhou-b \
    --CidrBlock 10.0.2.0/24 \
    --VSwitchName Public-b \
    --output text \
    --query VSwitchId)

# 创建安全组
SG_ID=$(aliyun ecs CreateSecurityGroup \
    --RegionId cn-hangzhou \
    --VpcId $VPC_ID \
    --SecurityGroupName WebServerSG \
    --Description "Security group for web servers" \
    --output text \
    --query SecurityGroupId)

# 添加安全组规则
aliyun ecs AuthorizeSecurityGroup \
    --RegionId cn-hangzhou \
    --SecurityGroupId $SG_ID \
    --IpProtocol tcp \
    --PortRange 22/22 \
    --SourceCidrIp 0.0.0.0/0

aliyun ecs AuthorizeSecurityGroup \
    --RegionId cn-hangzhou \
    --SecurityGroupId $SG_ID \
    --IpProtocol tcp \
    --PortRange 80/80 \
    --SourceCidrIp 0.0.0.0/0
```

### 4.3 计算服务（ECS）

```bash
# 创建密钥对
aliyun ecs CreateKeyPair \
    --RegionId cn-hangzhou \
    --KeyPairName MyKeyPair \
    --output text \
    --query PrivateKeyBody > MyKeyPair.pem
chmod 400 MyKeyPair.pem

# 查询镜像
IMAGE_ID=$(aliyun ecs DescribeImages \
    --RegionId cn-hangzhou \
    --ImageOwnerAlias system \
    --OSType linux \
    --Name "CentOS 7.9 64位" \
    --output text \
    --query 'Images.Image[0].ImageId')

# 创建实例
INSTANCE_ID=$(aliyun ecs RunInstances \
    --RegionId cn-hangzhou \
    --ImageId $IMAGE_ID \
    --InstanceType ecs.t6-c1m2.large \
    --SecurityGroupId $SG_ID \
    --VSwitchId $VSWITCH_1A \
    --InstanceName WebServer \
    --KeyPairName MyKeyPair \
    --InternetMaxBandwidthOut 10 \
    --output text \
    --query 'InstanceIdSets.InstanceIdSet[0]')

# 等待实例运行
aliyun ecs WaitForInstances \
    --RegionId cn-hangzhou \
    --InstanceIds "[$INSTANCE_ID]" \
    --Status Running

# 获取公网IP
PUBLIC_IP=$(aliyun ecs DescribeInstances \
    --RegionId cn-hangzhou \
    --InstanceIds "[$INSTANCE_ID]" \
    --output text \
    --query 'Instances.Instance[0].PublicIpAddress.IpAddress[0]')

echo "Instance: $INSTANCE_ID, IP: $PUBLIC_IP"
```

### 4.4 负载均衡（SLB）

```bash
# 创建SLB
SLB_ID=$(aliyun slb CreateLoadBalancer \
    --RegionId cn-hangzhou \
    --LoadBalancerName WebServerSLB \
    --VSwitchId $VSWITCH_1A \
    --PayType PayOnDemand \
    --output text \
    --query LoadBalancerId)

# 创建监听
aliyun slb CreateLoadBalancerTCPListener \
    --RegionId cn-hangzhou \
    --LoadBalancerId $SLB_ID \
    --ListenerPort 80 \
    --BackendServerPort 80 \
    --Bandwidth -1

# 添加后端服务器
aliyun slb AddBackendServers \
    --RegionId cn-hangzhou \
    --LoadBalancerId $SLB_ID \
    --BackendServers "[{\"ServerId\":\"$INSTANCE_ID\",\"Weight\":100}]"

# 启动监听
aliyun slb SetLoadBalancerStatus \
    --RegionId cn-hangzhou \
    --LoadBalancerId $SLB_ID \
    --ListenerPort 80 \
    --ListenerStatus running

# 获取SLB IP
SLB_IP=$(aliyun slb DescribeLoadBalancers \
    --RegionId cn-hangzhou \
    --LoadBalancerId $SLB_ID \
    --output text \
    --query 'LoadBalancers.LoadBalancer[0].Address')

echo "SLB IP: $SLB_IP"
```

### 4.5 对象存储（OSS）

```bash
# 创建Bucket
aliyun oss mb oss://my-bucket-name --region cn-hangzhou

# 上传文件
aliyun oss cp local-file.txt oss://my-bucket-name/

# 下载文件
aliyun oss cp oss://my-bucket-name/remote-file.txt ./

# 同步目录
aliyun oss sync ./local-folder oss://my-bucket-name/remote-folder

# 设置生命周期规则
aliyun oss lifecycle --method put oss://my-bucket-name file://lifecycle.xml

# lifecycle.xml
<?xml version="1.0" encoding="UTF-8"?>
<LifecycleConfiguration>
    <Rule>
        <ID>delete-old-files</ID>
        <Prefix>logs/</Prefix>
        <Status>Enabled</Status>
        <Expiration>
            <Days>30</Days>
        </Expiration>
    </Rule>
</LifecycleConfiguration>
```

---

## 6. 腾讯云实战

### 5.1 账号与CAM配置

```bash
# 安装腾讯云CLI
# pip install tccli

# 配置凭证(CAM访问密钥)
tccli configure
# secretId: AKID...
# secretKey: ...
# region: ap-guangzhou
# output: json

# 验证
tccli sts GetCallerIdentity
```

### 5.2 网络基础（VPC）

```bash
# 创建VPC
VPC_ID=$(tccli vpc CreateVpc \
    --Region ap-guangzhou \
    --CidrBlock "10.0.0.0/16" \
    --VpcName ProductionVPC \
    --output json | jq -r '.Vpc.VpcId')

# 创建子网
SUBNET_1A=$(tccli vpc CreateSubnet \
    --Region ap-guangzhou \
    --VpcId $VPC_ID \
    --SubnetName Public-1a \
    --CidrBlock "10.0.1.0/24" \
    --Zone ap-guangzhou-2 \
    --output json | jq -r '.Subnet.SubnetId')

# 创建安全组
SG_ID=$(tccli vpc CreateSecurityGroup \
    --Region ap-guangzhou \
    --GroupName WebServerSG \
    --GroupDescription "Security group for web servers" \
    --output json | jq -r '.SecurityGroup.SecurityGroupId')

# 添加规则
tccli vpc CreateSecurityGroupPolicies \
    --Region ap-guangzhou \
    --SecurityGroupId $SG_ID \
    --SecurityGroupPolicySet '{
        "Ingress": [{
            "PolicyIndex": 0,
            "Protocol": "tcp",
            "Port": "22",
            "CidrBlock": "0.0.0.0/0",
            "Action": "ACCEPT",
            "PolicyDescription": "Allow SSH"
        },{
            "PolicyIndex": 1,
            "Protocol": "tcp",
            "Port": "80,443",
            "CidrBlock": "0.0.0.0/0",
            "Action": "ACCEPT",
            "PolicyDescription": "Allow HTTP/HTTPS"
        }]
    }'
```

### 5.3 计算服务（CVM）

```bash
# 创建密钥对
tccli cvm CreateKeyPair --KeyName MyKeyPair --Region ap-guangzhou

# 查询镜像
IMAGE_ID=$(tccli cvm DescribeImages \
    --Region ap-guangzhou \
    --Filters '[{"Name":"image-type","Values":["PUBLIC_IMAGE"]},{"Name":"image-name","Values":["Ubuntu Server 22.04 LTS 64bit"]}]' \
    --output json | jq -r '.ImageSet[0].ImageId')

# 创建实例
INSTANCE_ID=$(tccli cvm RunInstances \
    --Region ap-guangzhou \
    --ImageId $IMAGE_ID \
    --InstanceChargeType POSTPAID_BY_HOUR \
    --InstanceType S2.MEDIUM4 \
    --SecurityGroupIds ["$SG_ID"] \
    --SubnetId $SUBNET_1A \
    --InstanceName WebServer \
    --KeyIds ["MyKeyPair"] \
    --InternetAccessible '{"InternetChargeType":"TRAFFIC_POSTPAID_BY_HOUR","InternetMaxBandwidthOut":10,"PublicIpAssigned":true}' \
    --output json | jq -r '.InstanceIdSet[0]')

# 等待实例运行
tccli cvm DescribeInstancesStatus \
    --Region ap-guangzhou \
    --InstanceIds ["$INSTANCE_ID"]

# 获取公网IP
PUBLIC_IP=$(tccli cvm DescribeInstances \
    --Region ap-guangzhou \
    --InstanceIds ["$INSTANCE_ID"] \
    --output json | jq -r '.InstanceSet[0].PublicIpAddresses[0]')

echo "Instance: $INSTANCE_ID, IP: $PUBLIC_IP"
```

### 5.4 负载均衡（CLB）

```bash
# 创建CLB
CLB_ID=$(tccli clb CreateLoadBalancer \
    --Region ap-guangzhou \
    --LoadBalancerType PUBLIC \
    --LoadBalancerName WebServerCLB \
    --VpcId $VPC_ID \
    --SubnetId $SUBNET_1A \
    --output json | jq -r '.LoadBalancerIds[0]')

# 创建监听器
tccli clb CreateListener \
    --Region ap-guangzhou \
    --LoadBalancerId $CLB_ID \
    --Ports '[{"Protocol":"HTTP","Port":80}]' \
    --ListenerNames '["HTTPListener"]'

# 绑定后端服务器
tccli clb RegisterTargets \
    --Region ap-guangzhou \
    --LoadBalancerId $CLB_ID \
    --Listeners '[{"Protocol":"HTTP","Port":80,"Targets":[{"InstanceId":"'$INSTANCE_ID'","Port":80,"Weight":100}]}]'

# 获取CLB IP
CLB_IP=$(tccli clb DescribeLoadBalancers \
    --Region ap-guangzhou \
    --LoadBalancerIds ["$CLB_ID"] \
    --output json | jq -r '.LoadBalancerSet[0].LoadBalancerVips[0]')

echo "CLB IP: $CLB_IP"
```

### 5.5 对象存储（COS）

```bash
# 创建Bucket
tccli cos mb cos://my-bucket-1250000000 -r ap-guangzhou

# 上传文件
tccli cos cp local-file.txt cos://my-bucket-1250000000/

# 下载文件
tccli cos cp cos://my-bucket-1250000000/remote-file.txt ./

# 同步目录
tccli cos sync ./local-folder cos://my-bucket-1250000000/remote-folder

# 生成临时密钥（1小时有效）
tccli sts GetFederationToken \
    --Name temp-user \
    --DurationSeconds 3600 \
    --Policy '{"version":"2.0","statement":[{"effect":"allow","action":["cos:GetObject"],"resource":["qcs::cos:ap-guangzhou:uid/1250000000:my-bucket-1250000000/*"]}]}'
```

---

## 6. 华为云实战

### 6.1 账号与IAM配置

```bash
# 安装华为云CLI
# https://support.huaweicloud.com/devg-cli/cli_01_0001.html

# 配置凭证(IAM访问密钥)
hcloud configure
# ak: ...
# sk: ...
# region: cn-north-4
# project-id: ...

# 验证
hcloud iam show-project
```

### 6.2 网络基础（VPC）

```bash
# 创建VPC
VPC_ID=$(hcloud vpc create \
    --name ProductionVPC \
    --cidr 10.0.0.0/16 \
    --region cn-north-4 \
    --query 'vpc.id' -o tsv)

# 创建子网
SUBNET_1A=$(hcloud vpc subnet create \
    --vpc-id $VPC_ID \
    --name Public-1a \
    --cidr 10.0.1.0/24 \
    --gateway 10.0.1.1 \
    --az cn-north-4a \
    --query 'subnet.id' -o tsv)

# 创建安全组
SG_ID=$(hcloud vpc security-group create \
    --name WebServerSG \
    --vpc-id $VPC_ID \
    --query 'security_group.id' -o tsv)

# 添加规则
hcloud vpc security-group rule create \
    --security-group-id $SG_ID \
    --direction ingress \
    --protocol tcp \
    --port 22 \
    --remote-ip 0.0.0.0/0

hcloud vpc security-group rule create \
    --security-group-id $SG_ID \
    --direction ingress \
    --protocol tcp \
    --port 80,443 \
    --remote-ip 0.0.0.0/0
```

### 6.3 计算服务（ECS）

```bash
# 创建密钥对
hcloud ecs keypair create --name MyKeyPair

# 查询镜像
IMAGE_ID=$(hcloud ims image list \
    --name "Ubuntu 22.04 server 64bit" \
    --visibility public \
    --query 'images[0].id' -o tsv)

# 创建实例
INSTANCE_ID=$(hcloud ecs server create \
    --name WebServer \
    --image-id $IMAGE_ID \
    --flavor s6.large.2 \
    --vpc-id $VPC_ID \
    --subnet-id $SUBNET_1A \
    --security-groups $SG_ID \
    --key-name MyKeyPair \
    --eip-bandwidth 10 \
    --query 'server.id' -o tsv)

# 等待实例运行
hcloud ecs server wait --id $INSTANCE_ID --status ACTIVE

# 获取公网IP
PUBLIC_IP=$(hcloud ecs server show \
    --id $INSTANCE_ID \
    --query 'server.addresses.\"ProductionVPC\"[?"OS-EXT-IPS:type"=='\''floating'\''].addr' -o tsv)

echo "Instance: $INSTANCE_ID, IP: $PUBLIC_IP"
```

### 6.4 负载均衡（ELB）

```bash
# 创建ELB
ELB_ID=$(hcloud elb loadbalancer create \
    --name WebServerELB \
    --vpc-id $VPC_ID \
    --subnet-id $SUBNET_1A \
    --bandwidth 10 \
    --type public \
    --query 'loadbalancer.id' -o tsv)

# 创建监听器
LISTENER_ID=$(hcloud elb listener create \
    --loadbalancer-id $ELB_ID \
    --protocol HTTP \
    --port 80 \
    --query 'listener.id' -o tsv)

# 创建后端服务器组
POOL_ID=$(hcloud elb pool create \
    --listener-id $LISTENER_ID \
    --protocol HTTP \
    --lb-algorithm ROUND_ROBIN \
    --query 'pool.id' -o tsv)

# 添加后端服务器
hcloud elb member create \
    --pool-id $POOL_ID \
    --address $PUBLIC_IP \
    --protocol-port 80 \
    --weight 100

# 获取ELB IP
ELB_IP=$(hcloud elb loadbalancer show \
    --id $ELB_ID \
    --query 'loadbalancer.vip_address' -o tsv)

echo "ELB IP: $ELB_IP"
```

### 6.5 对象存储（OBS）

```bash
# 创建Bucket
hcloud obs bucket create \
    --bucket my-bucket-name \
    --region cn-north-4 \
    --acl public-read

# 上传文件
hcloud obs object upload \
    --bucket my-bucket-name \
    --key remote-file.txt \
    --file local-file.txt

# 下载文件
hcloud obs object download \
    --bucket my-bucket-name \
    --key remote-file.txt \
    --file ./local-file.txt

# 生成临时URL（1小时有效）
hcloud obs object generate-url \
    --bucket my-bucket-name \
    --key remote-file.txt \
    --expires 3600
```

---

## 7. 跨云对比与迁移

### 7.1 架构对比

| 维度 | AWS | Azure | Google Cloud | 阿里云 | 腾讯云 | 华为云 |
|------|-----|-------|--------------|--------|--------|--------|
| **网络模型** | VPC+子网 | VNet+子网 | VPC+子网 | VPC+交换机 | VPC+子网 | VPC+子网 |
| **安全组** | 有状态 | NSG有状态 | 防火墙规则 | 有状态 | 有状态 | 有状态 |
| **负载均衡** | ALB/NLB | Load Balancer | Cloud LB | SLB | CLB | ELB |
| **公网IP** | Elastic IP | Public IP | 全球任播IP | EIP | 公网IP | 弹性IP |
| **NAT网关** | NAT Gateway | NAT Gateway | Cloud NAT | NAT网关 | NAT网关 | NAT网关 |

### 7.2 迁移策略

```
迁移方式选择:
├── 同构迁移（相同架构）
│   ├── 应用层: 容器化后跨云部署
│   ├── 数据层: DTS/数据库同步工具
│   └── 文件层: 对象存储跨云复制
└── 异构迁移（不同架构）
    ├── 重新部署应用
    ├── 数据导出导入
    └── DNS切换流量
```

### 7.3 多云管理工具

| 工具 | 功能 | 适用场景 |
|------|------|----------|
| **Terraform** | 多云资源编排 | 基础设施即代码(IaC) |
| **Crossplane** | K8s多云控制平面 | 已有K8s环境 |
| **Ansible** | 多云配置管理 | 应用部署 |
| **Cost Explorer** | 多云成本分析 | FinOps |

---

## 8. 价格参考表

> ⚠️ **重要提示**: 以下价格为2025年Q1参考价格，实际价格请以各云厂商官网为准
> 📅 **下次更新**: 2025年Q2（4月）

### 8.1 计算实例价格对比（2核4G配置）

| 云厂商 | 实例类型 | 按量付费(小时) | 包年包月(月) | 备注 |
|--------|----------|----------------|--------------|------|
| **AWS** | t3.medium | $0.0416 (~¥0.30) | ~$30 (~¥215) | 突发性能，基准20% |
| **Azure** | Standard_B2s | $0.0416 (~¥0.30) | ~$30 (~¥215) | 可突增，基准40% |
| **Google Cloud** | e2-medium | $0.0336 (~¥0.24) | ~$24 (~¥172) | 通用型，可突增 |
| **阿里云** | ecs.t6-c1m2.large | ¥0.34 | ¥150 | 突发性能，基准20% |
| **腾讯云** | S2.MEDIUM4 | ¥0.32 | ¥140 | 标准型，稳定性能 |
| **华为云** | s6.large.2 | ¥0.34 | ¥145 | 通用型，稳定性能 |

### 8.2 对象存储价格对比

| 云厂商 | 存储单价(GB/月) | 流量单价(GB) | 请求费用(万次) | 备注 |
|--------|-----------------|--------------|----------------|------|
| **AWS S3** | $0.023 (~¥0.17) | $0.09 (~¥0.65) | $0.0004 (~¥0.003) | 标准存储 |
| **Azure Blob** | $0.0184 (~¥0.13) | $0.087 (~¥0.63) | $0.004 (~¥0.03) | 热存储 |
| **Google Cloud Storage** | $0.020 (~¥0.14) | $0.12 (~¥0.86) | $0.0004 (~¥0.003) | 标准存储 |
| **阿里云OSS** | ¥0.12 | ¥0.50 | ¥0.01 | 标准存储 |
| **腾讯云COS** | ¥0.118 | ¥0.50 | ¥0.01 | 标准存储 |
| **华为云OBS** | ¥0.099 | ¥0.45 | ¥0.01 | 标准存储 |

### 8.3 负载均衡价格对比

| 云厂商 | 实例费(月) | 流量费(GB) | 新建连接费(万) | 备注 |
|--------|------------|------------|----------------|------|
| **AWS ALB** | $16.43 (~¥118) | $0.008/LCU | - | 按LCU计费 |
| **Azure LB** | 免费 | $0.025 (~¥0.18) | - | 标准版 |
| **Google Cloud LB** | ~$18 (~¥129) | $0.008/LCU | - | 按转发规则+LCU计费 |
| **阿里云SLB** | ¥15-100 | ¥0.80 | ¥0.02 | 按规格计费 |
| **腾讯云CLB** | ¥18-100 | ¥0.80 | ¥0.02 | 按规格计费 |
| **华为云ELB** | ¥18-100 | ¥0.80 | ¥0.02 | 按规格计费 |

### 8.4 数据库价格对比（MySQL 2核4G）

| 云厂商 | 按量付费(小时) | 包年包月(月) | 存储(GB/月) | 备注 |
|--------|----------------|--------------|-------------|------|
| **AWS RDS** | $0.068 (~¥0.49) | ~$50 (~¥360) | $0.115 | MySQL 8.0 |
| **Azure SQL** | $0.073 (~¥0.53) | ~$53 (~¥380) | $0.115 | 兼容MySQL |
| **Google Cloud SQL** | $0.055 (~¥0.40) | ~$40 (~¥286) | $0.17 | MySQL 8.0 |
| **阿里云RDS** | ¥0.56 | ¥245 | ¥0.50 | MySQL 8.0 |
| **腾讯云TencentDB** | ¥0.52 | ¥228 | ¥0.50 | MySQL 8.0 |
| **华为云RDS** | ¥0.54 | ¥236 | ¥0.50 | MySQL 8.0 |

### 8.5 带宽价格对比（1Mbps）

| 云厂商 | 按量付费(GB) | 包年包月(月) | 备注 |
|--------|--------------|--------------|------|
| **AWS** | $0.09 (~¥0.65) | - | 按流量计费 |
| **Azure** | $0.087 (~¥0.63) | - | 按流量计费 |
| **Google Cloud** | $0.12 (~¥0.86) | - | 按流量计费，出站免费额度1GB/月 |
| **阿里云** | ¥0.80 | ¥23 | 可选按量/固定 |
| **腾讯云** | ¥0.80 | ¥20 | 可选按量/固定 |
| **华为云** | ¥0.80 | ¥18 | 可选按量/固定 |

### 8.6 免费额度对比

| 云厂商 | 计算 | 存储 | 数据库 | 时长 |
|--------|------|------|--------|------|
| **AWS** | EC2 t2.micro 750小时/月 | S3 5GB | RDS 750小时 | 12个月 |
| **Azure** | VM B1s 750小时/月 | Blob 5GB | SQL 250GB | 12个月 |
| **Google Cloud** | e2-micro 750小时/月 | Cloud Storage 5GB | Cloud SQL 无 | 12个月+部分长期 |
| **阿里云** | ECS 1核1G | OSS 10GB | RDS 1个月 | 部分长期 |
| **腾讯云** | CVM 1核1G 1个月 | COS 50GB | - | 部分长期 |
| **华为云** | ECS 1核1G 1个月 | OBS 5GB | - | 部分长期 |

---

## 9. 附录：CLI工具速查

### 9.1 安装命令速查

| 工具 | Windows安装 | Linux安装 | macOS安装 |
|------|-------------|-----------|-----------|
| **AWS CLI** | `msiexec` / 官网下载 | `pip install awscli` | `brew install awscli` |
| **Azure CLI** | `winget install` | `curl -sL https://aka.ms/InstallAzureCLIDeb \| sudo bash` | `brew install azure-cli` |
| **Google Cloud CLI** | 官网下载 | `curl -sL https://sdk.cloud.google.com | bash` | `brew install --cask google-cloud-sdk` |
| **阿里云CLI** | 官网下载 | `curl -sL https://aliyun-cli | bash` | `brew install aliyun-cli` |
| **腾讯云CLI** | `pip install tccli` | `pip install tccli` | `pip install tccli` |
| **华为云CLI** | 官网下载 | `curl -sL https://cli.huaweicloud.com | bash` | `brew install hcloud` |

### 9.2 常用命令对比

| 操作 | AWS | Azure | Google Cloud | 阿里云 | 腾讯云 | 华为云 |
|------|-----|-------|--------------|--------|--------|--------|
| **登录** | `aws configure` | `az login` | `gcloud init` | `aliyun configure` | `tccli configure` | `hcloud configure` |
| **查看VM** | `aws ec2 describe-instances` | `az vm list` | `gcloud compute instances list` | `aliyun ecs DescribeInstances` | `tccli cvm DescribeInstances` | `hcloud ecs server list` |
| **创建VM** | `aws ec2 run-instances` | `az vm create` | `gcloud compute instances create` | `aliyun ecs RunInstances` | `tccli cvm RunInstances` | `hcloud ecs server create` |
| **查看VPC** | `aws ec2 describe-vpcs` | `az network vnet list` | `gcloud compute networks list` | `aliyun vpc DescribeVpcs` | `tccli vpc DescribeVpcs` | `hcloud vpc list` |
| **查看Bucket** | `aws s3 ls` | `az storage account list` | `gcloud storage ls` | `aliyun oss ls` | `tccli cos ls` | `hcloud obs bucket list` |

### 9.3 价格查询CLI

```bash
# AWS 价格查询
aws pricing get-products \
    --service-code AmazonEC2 \
    --filters '[{"Field":"instanceType","Value":"t3.medium","Type":"TERM_MATCH"}]'

# Azure 价格查询
az consumption usage list --billing-period-name 202501

# 阿里云价格查询
aliyun bssopenapi QueryAccountBalance

# 腾讯云价格查询
tccli billing DescribeAccountBalance

# 华为云价格查询
hcloud bss show-account-balances

# Google Cloud价格查询
gcloud billing budgets list --billing-account=XXXXXX-XXXXXX-XXXXXX
gcloud billing accounts list
```

---

> **文档维护**: 本模块价格表每季度更新一次
> 
> **更新历史**:
> - 2025-Q1: 初始版本，包含AWS/Azure/阿里云/腾讯云/华为云
> - 2025-Q2: 新增Google Cloud实战全套件（Compute Engine、GKE、Cloud Run、Cloud SQL、Cloud Storage、Cloud Load Balancing、Cloud Operations等）
> 
> **反馈建议**: 如发现价格信息过期，请提醒我更新
