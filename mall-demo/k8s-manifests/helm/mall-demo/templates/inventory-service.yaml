{{- if .Values.inventoryService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: inventory-service
    {{- include "mall-demo.labels" (dict "name" "inventory-service" "Chart" .Chart "Release" .Release) | nindent 4 }}
spec:
  replicas: {{ .Values.inventoryService.replicaCount }}
  selector:
    matchLabels:
      app: inventory-service
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: inventory-service
      annotations:
        {{- include "mall-demo.prometheusAnnotations" (dict "metricsPath" .Values.serviceMonitor.path) | nindent 8 }}
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name }}
      containers:
        - name: inventory-service
          image: {{ .Values.global.imageRegistry }}/{{ .Values.imagePrefix }}-{{ .Values.inventoryService.image }}:{{ .Values.global.imageTag }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
          env:
            - name: SERVER_PORT
              value: "8080"
            {{- include "mall-demo.dbEnv" (dict "Values" .Values "dbName" .Values.inventoryService.dbName) | nindent 12 }}
            {{- include "mall-demo.redisEnv" (dict "Values" .Values) | nindent 12 }}
            {{- include "mall-demo.otelEnv" (dict "Values" .Values "serviceName" "inventory-service") | nindent 12 }}
          resources:
            {{- toYaml .Values.inventoryService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
      {{- include "mall-demo.topologySpread" (dict "Values" .Values "serviceName" "inventory-service") | nindent 6 }}
      {{- include "mall-demo.antiAffinity" (dict "serviceName" "inventory-service") | nindent 6 }}
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: inventory-service
    {{- include "mall-demo.labels" (dict "name" "inventory-service" "Chart" .Chart "Release" .Release) | nindent 4 }}
spec:
  type: {{ .Values.inventoryService.service.type }}
  ports:
    - port: {{ .Values.inventoryService.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: inventory-service
{{- end }}
