{{- if .Values.paymentService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: payment-service
    {{- include "mall-demo.labels" (dict "name" "payment-service" "Chart" .Chart "Release" .Release) | nindent 4 }}
spec:
  replicas: {{ .Values.paymentService.replicaCount }}
  selector:
    matchLabels:
      app: payment-service
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: payment-service
      annotations:
        {{- include "mall-demo.prometheusAnnotations" (dict "metricsPath" .Values.serviceMonitor.path) | nindent 8 }}
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name }}
      containers:
        - name: payment-service
          image: {{ .Values.global.imageRegistry }}/{{ .Values.imagePrefix }}-{{ .Values.paymentService.image }}:{{ .Values.global.imageTag }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
          env:
            - name: SERVER_PORT
              value: "8080"
            {{- include "mall-demo.dbEnv" (dict "Values" .Values "dbName" .Values.paymentService.dbName) | nindent 12 }}
            {{- include "mall-demo.otelEnv" (dict "Values" .Values "serviceName" "payment-service") | nindent 12 }}
          resources:
            {{- toYaml .Values.paymentService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
      {{- include "mall-demo.topologySpread" (dict "Values" .Values "serviceName" "payment-service") | nindent 6 }}
      {{- include "mall-demo.antiAffinity" (dict "serviceName" "payment-service") | nindent 6 }}
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: payment-service
    {{- include "mall-demo.labels" (dict "name" "payment-service" "Chart" .Chart "Release" .Release) | nindent 4 }}
spec:
  type: {{ .Values.paymentService.service.type }}
  ports:
    - port: {{ .Values.paymentService.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: payment-service
{{- end }}
